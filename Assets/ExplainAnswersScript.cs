using System.Collections;
using System.Collections.Generic;
using TMPro;
using UnityEngine;

public class ExplainAnswersScript : MonoBehaviour
{
    // Start is called before the first frame update
    [SerializeField] TextMeshProUGUI _answerText1, _answerText2, _answerText3;

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
